Gemfile.lock
.vscode/
.claude/
.bundle/
.omo/
tmp/
# Anchored: the cloned-source cache is the one at the repo root. Unanchored,
# this pattern also matched lib/gost_fetcher/sources/, which is library code.
/sources/
pdfs/
TODO.impl/
spec/examples.txt
*.gem
